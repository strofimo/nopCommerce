
<script>
    $(function () {
        var kTabs = $('#product-edit').data('kendoTabStrip');
        var selectedStores = $('#pnl-available-stores:has(input:checked)');
        selectedStores.ForEach(function(data)
        {

        })


        kTabs.append({
            text: '<strong id="StockPricerPerStoreTabId">Price per store</strong>',
            encoded: false,
            contentUrl: '@Url.Action( "EditStockPricerPerStore", "StockPricerPerStore", new RouteValueDictionary { { "Namespaces", "Nop.Plugin.Misc.StockPricerPerStore.Controllers" }, { "area", null }, { "productId", Model.Id } })'
        });
        setVisibleStockPricerPerStoreTab();
       /* $('#LimitedToStores').on('change', setVisibleStockPricerPerStoreTab);*/
        /*$('#pnl-available-stores input').on('change', setVisibleStockPricerPerStoreTab);*/
        function setVisibleStockPricerPerStoreTab()
        {
            if ($('#LimitedToStores').is(':checked') && $('#pnl-available-stores:has(input:checked)').length > 0) {
                $('li:has(#StockPricerPerStoreTabId)').show();
                $('#group-prices').hide();
            }
            else {
                $('li:has(#StockPricerPerStoreTabId)').hide();
                $('#group-prices').show();
            }
        }
    });
    //# sourceURL=dynamicScript3.js
</script>
